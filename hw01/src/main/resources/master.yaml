databaseChangeLog:
  - changeSet:
      id: create_table
      author: arozhnev
      changes:
        - sql:
            sql: "CREATE TABLE t_user(
              id     varchar(100) PRIMARY KEY,
              name  varchar(100) not null
            );"